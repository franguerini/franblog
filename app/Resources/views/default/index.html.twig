{% extends 'base_front.html.twig' %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-md-8">
                Posts
                {% for post in posts %}
                    <div class="col-md-6">
                        <h3>
                            {{ post.titulo }}
                        </h3>
                        {{ post.contenido }}
                    </div>
                {% endfor %}
            </div>
            <div class="col-md-4">
                Autores

            </div>

        </div>
    </div>

{% endblock %}

{% block stylesheets %}

{% endblock %}
